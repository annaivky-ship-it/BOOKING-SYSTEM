import AdminLogin from '../components/AdminLogin';

export default function LoginPage() {
  return (
    <main className="container" style={{ minHeight: '100vh', paddingTop: '2rem' }}>
      <AdminLogin />
    </main>
  );
}